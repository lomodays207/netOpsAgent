# 实施计划

- [x] 1. 编写 bug 条件探索测试
  - **属性 1: Bug 条件** - 访问关系数据查询在未修复代码上执行 RAG
  - **关键**: 此测试在未修复代码上必须失败 - 失败确认 bug 存在
  - **不要尝试修复测试或代码当它失败时**
  - **注意**: 此测试编码了期望行为 - 它将在实施后通过时验证修复
  - **目标**: 暴露证明 bug 存在的反例
  - **范围化 PBT 方法**: 将属性范围限定为具体的失败案例（带 `use_rag=True` 的访问关系数据查询）
  - 测试访问关系数据查询（例如 "N-CRM有哪些访问关系"、"CRMJS_AP部署单元有哪些访问关系"）在未修复代码上使用 `use_rag=True` 时执行 RAG 检索
  - 测试断言应验证：
    - RAG 检索被执行（发出 rag_start 和 rag_result 事件）
    - 未发出 rag_skipped 事件
    - 这确认了 bug：数据查询错误地触发了 RAG
  - 包含的测试用例：
    - 系统编码查询："N-CRM有哪些访问关系" 使用 `use_rag=True`
    - 部署单元查询："CRMJS_AP部署单元有哪些访问关系" 使用 `use_rag=True`
    - 入向查询："哪些系统访问N-OA" 使用 `use_rag=True`
  - 在未修复代码上运行测试
  - **预期结果**: 测试失败（这是正确的 - 它证明 bug 存在）
  - 记录发现的反例（例如 "对 'N-CRM有哪些访问关系' 执行了 RAG 检索而不是跳过"）
  - 当测试编写完成、运行并记录失败时标记任务完成
  - _需求: 1.1, 1.2, 1.3, 1.4_

- [x] 2. 编写保持性属性测试（在实施修复之前）
  - **属性 2: 保持性** - 非数据查询的 RAG 行为
  - **重要**: 遵循观察优先方法论
  - 在未修复代码上观察非 bug 输入的行为（知识查询、一般问题、use_rag=False）
  - 编写基于属性的测试，捕获保持性需求中观察到的行为模式
  - 基于属性的测试生成许多测试用例以提供更强的保证
  - 包含的测试用例：
    - 知识查询："访问关系如何开权限" 使用 `use_rag=True` → RAG 应执行（观察并验证）
    - 一般问题："如何排查网络故障" 使用 `use_rag=True` → RAG 应执行（观察并验证）
    - RAG 禁用：任何消息使用 `use_rag=False` → RAG 应跳过（观察并验证）
    - RAG 错误：模拟 RAG 错误 → 应发出 rag_error 事件并继续（观察并验证）
    - 混合查询："N-CRM有哪些访问关系？另外访问关系如何开权限？" 使用 `use_rag=True` → 观察当前行为
  - 在未修复代码上运行测试
  - **预期结果**: 测试通过（这确认了要保持的基线行为）
  - 当测试编写完成、运行并在未修复代码上通过时标记任务完成
  - _需求: 3.1, 3.2, 3.3, 3.4, 3.5_

- [x] 3. 访问关系数据查询的 RAG 跳过修复

  - [x] 3.1 实现检测函数 `is_access_relation_data_query`
    - 在 `src/api.py` 中的 `general_chat_stream_v2` 之前创建辅助函数
    - 实现系统标识符的正则表达式模式：`(N-[A-Z]+|P-[A-Z-]+|[A-Z]+JS_[A-Z]+|客户关系管理系统|办公自动化系统|部署单元)`
    - 实现关系查询关键词的正则表达式模式：`(有哪些访问关系|哪些系统访问|被.*访问|访问.*系统|之间.*访问关系)`
    - 实现知识查询关键词的正则表达式模式：`(如何|怎么|流程|权限|提单|申请|审批|管理)`
    - 仅当满足以下条件时返回 `True`：has_system_identifier AND asks_for_relations AND NOT asks_for_knowledge
    - 为检测函数编写单元测试，测试各种消息模式
    - _Bug_Condition: isBugCondition(input) 其中 input.use_rag=True AND is_access_relation_data_query(input.message)=True_
    - _Expected_Behavior: is_access_relation_data_query 对数据查询返回 True，对知识查询返回 False_
    - _Preservation: 检测函数不应影响现有代码路径_
    - _需求: 2.1, 2.2, 2.3, 2.4_

  - [x] 3.2 在 `general_chat_stream_v2` 中添加预处理逻辑
    - 在 RAG 检索块之前添加检测逻辑（在 `src/api.py` 的第 ~1820 行之前）
    - 检查 `request.use_rag` 是否为 `True` 且 `is_access_relation_data_query(request.message)` 是否为 `True`
    - 如果两个条件都满足，设置标志 `skip_rag = True` 并发出 `rag_skipped` 事件
    - 事件格式：`{"type": "rag_skipped", "reason": "检测到访问关系数据查询,跳过知识库检索"}`
    - 否则，设置 `skip_rag = False`
    - _Bug_Condition: 来自设计的 isBugCondition(input)_
    - _Expected_Behavior: skip_rag 标志根据消息内容正确设置_
    - _Preservation: 预处理不应影响非数据查询_
    - _需求: 2.1, 2.5_

  - [x] 3.3 修改 RAG 检索块以遵守跳过标志
    - 将现有 RAG 检索逻辑（第 ~1820-1835 行）包装在条件检查中
    - 仅当 `request.use_rag` 为 `True` 且 `skip_rag` 为 `False` 时执行 RAG
    - 保留所有现有 RAG 逻辑（检索、事件发出、错误处理）
    - 确保在跳过 RAG 时正确初始化 `system_prompt` 和 `retrieved_docs`
    - _Bug_Condition: 来自设计的 isBugCondition(input)_
    - _Expected_Behavior: 访问关系数据查询跳过 RAG 检索_
    - _Preservation: 知识查询和一般问题继续执行 RAG 检索_
    - _需求: 2.1, 2.2, 2.3, 2.4, 2.5, 3.1, 3.2, 3.3, 3.4_

  - [x] 3.4 验证 bug 条件探索测试现在通过
    - **属性 1: 期望行为** - 访问关系数据查询在修复代码上跳过 RAG
    - **重要**: 重新运行任务 1 中的相同测试 - 不要编写新测试
    - 任务 1 中的测试编码了期望行为（但对修复代码使用反转的断言）
    - 更新测试断言以验证：
      - 未执行 RAG 检索（未发出 rag_start 和 rag_result 事件）
      - 发出了 rag_skipped 事件并带有正确原因
      - 工具调用正常进行（最终调用 query_access_relations）
    - 使用更新的断言运行步骤 1 中的 bug 条件探索测试
    - **预期结果**: 测试通过（确认 bug 已修复）
    - _需求: 2.1, 2.2, 2.3, 2.4, 2.5_

  - [x] 3.5 验证保持性测试仍然通过
    - **属性 2: 保持性** - 非数据查询的 RAG 行为不变
    - **重要**: 重新运行任务 2 中的相同测试 - 不要编写新测试
    - 运行步骤 2 中的保持性属性测试
    - **预期结果**: 测试通过（确认无回归）
    - 确认修复后所有测试仍然通过（无回归）
    - 验证知识查询仍然执行 RAG
    - 验证一般问题仍然执行 RAG
    - 验证 use_rag=False 仍然跳过 RAG
    - 验证 RAG 错误处理不变
    - _需求: 3.1, 3.2, 3.3, 3.4, 3.5_

- [x] 4. 检查点 - 确保所有测试通过
  - 运行 `is_access_relation_data_query` 函数的所有单元测试
  - 运行 bug 条件和保持性的所有基于属性的测试
  - 运行完整流程的集成测试（访问关系数据查询和知识查询）
  - 验证事件发出顺序和内容
  - 验证 RAG 跳过后的工具调用行为
  - 确保所有测试通过，如有问题请询问用户
